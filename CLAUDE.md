# Claude Code bridge

Read `AGENTS.md` first — it is the source of truth for the rules that govern
every change to this repo, including the non-negotiable spend-approval
rules. This file exists only so Claude Code loads it automatically; it
intentionally does not duplicate the content.
